#!/usr/bin/env bash

declare image="${1:-neovim-tmux-explorations-neovim}"
docker run \
    --platform linux/amd64 \
    --volume ~/work:/root/work \
    --name test \
    --rm \
    -i \
    -t \
    "$image"

